#include "main.h"
/**
 *print_rev - print string in reverse
 *@s: The string to print
 *Return: void
 */
int print_rev(char *str)
{
	int c = 0;
	while (s[c] != '\0')
	{
	c++;
	}
	for (c -= 1; c>= 0; c--)
	{
	_putchar(s[c]);
	}
	_putchar('\n');
}
